apiVersion: v1
kind: Service
metadata:
  #name: postgres
  name: db
  namespace: dev-myapp
  labels:
    app: db
spec:
  #type: NodePort
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: db